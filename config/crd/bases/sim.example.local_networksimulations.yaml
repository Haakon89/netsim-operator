---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: networksimulations.sim.example.local
spec:
  group: sim.example.local
  names:
    kind: NetworkSimulation
    listKind: NetworkSimulationList
    plural: networksimulations
    singular: networksimulation
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: NetworkSimulation is the Schema for the networksimulations API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of NetworkSimulation
            properties:
              deviceCount:
                type: integer
              deviceImage:
                type: string
              traffic:
                description: |-
                  EDIT THIS FILE!  THIS IS SCAFFOLDING FOR YOU TO OWN!
                  NOTE: json tags are required.  Any new fields you add must have json tags for the fields to be serialized.
                properties:
                  steps:
                    items:
                      properties:
                        count:
                          type: integer
                        from:
                          type: string
                        to:
                          type: string
                        type:
                          type: string
                      required:
                      - to
                      - type
                      type: object
                    type: array
                type: object
            required:
            - deviceCount
            type: object
          status:
            description: status defines the observed state of NetworkSimulation
            properties:
              namespace:
                type: string
              phase:
                type: string
              trafficJobName:
                type: string
            required:
            - trafficJobName
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
